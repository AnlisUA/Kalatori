name: Merge to main

on:
  push:
    branches:
      - main

jobs:
  cargo-test:
    uses: ./.github/workflows/_job-cargo-test.yml

  integration-test:
    uses: ./.github/workflows/_job-integration-test.yml
